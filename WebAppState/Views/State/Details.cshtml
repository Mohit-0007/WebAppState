@model State

@{
    ViewData["Title"] = "State Details";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <tr>
        <th>Id</th>
        <th>Name</th>
        
<tr>
    <tr>
        <td>@Model.Id</td>
        <td>@Model.Name</td>
</tr>
</table>

<a asp-action="index" asp-controller="State" class="btn btn-dark">BACK TO HOME</a>
